apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet60-aut-prm.fullname" . }}
  labels:
    {{- include "dotnet60-aut-prm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dotnet60-aut-prm.selectorLabels" . | nindent 4 }}
