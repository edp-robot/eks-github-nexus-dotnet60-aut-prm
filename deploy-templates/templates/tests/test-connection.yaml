apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dotnet60-aut-prm.fullname" . }}-test-connection"
  labels:
    {{- include "dotnet60-aut-prm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dotnet60-aut-prm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
